pipeline {
   
    agent { label 'salam' }

    stages {
        stage('Initialize'){
            steps{
                echo "Start Initialize"
            }
        }
        stage('Build'){
            steps{
                echo "Start Build"
            }
        }
       stage('Deploy'){
            steps{
                echo "Start Deployment"
             }
        }

    }
}
