pipeline {
   
    agent { label 'docker' }

    stages {
        stage('Initialize'){
            steps{
                echo "Start Initialize"
            }
        }
        stage('Build'){
            steps{
                echo "Start Build"
            }
        }
    }
}
